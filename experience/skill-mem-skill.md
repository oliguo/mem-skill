## Building mem-skill from scratch
**Date:** 2026-02-19
**Skill:** mem-skill
**Source:** conversation
**Context:** Created a self-evolving memory skill for AI agents from two reference repos: Toolsai/auto-skill (5-step core loop pattern) and tobi/qmd (BM25+vector search engine). The skill needed to follow the skill-creator framework conventions exactly.
**Solution:**
- Studied auto-skill's 5-step core loop: keyword extract → topic switch → experience read → knowledge read → proactive record
- Adapted the Chinese-language auto-skill patterns into English
- Made QMD engine optional (default engine = JSON file scanning, QMD = advanced)
- Created pluggable engine architecture via `references/engines.md`
- Added init.sh with `--mem-engine=qmd` flag for optional QMD setup
**Key Files/Paths:**
- SKILL.md (core skill definition, 271 lines)
- scripts/init.sh (initialization with engine selection)
- references/engines.md (engine abstraction docs)
- references/qmd-engine.md (QMD-specific commands)
**Related:**
**Keywords:** mem-skill, auto-skill, qmd, architecture, core-loop, init

## Fixing skill-creator validation errors
**Date:** 2026-02-19
**Skill:** mem-skill
**Source:** conversation
**Context:** Official `quick_validate.py` rejected the SKILL.md because angle brackets `<user>` in the description field. Also discovered macOS `head -n -1` incompatibility in test scripts.
**Solution:**
- Removed `Install with: npx skills add <user>/mem-skill.` from frontmatter description
- Replaced `head -n -1` with awk-based YAML body extraction for macOS compatibility
- Added `--exclude-dir='.venv'` to security grep to avoid false positives from pip internals
- Installed pyyaml in venv for validator
**Key Files/Paths:**
- SKILL.md (line 3-4, description field)
- tests/run_tests.sh (macOS-compatible awk extraction)
**Related:** [[workflow#Skill Validator: No Angle Brackets in YAML Frontmatter]], [[workflow#macOS Compatibility: head and sed Differences]]
**Keywords:** validation, angle-brackets, macOS, head, sed, quick_validate

## Topic switch threshold: inclusive boundary
**Date:** 2026-02-19
**Skill:** mem-skill
**Source:** conversation
**Context:** Integration test 07-topic-switch revealed that the overlap threshold for topic switch detection used strict `< 0.6` instead of `<= 0.6`. When exactly 40% of keywords change (60% overlap), the switch should trigger per the SKILL.md spec ">=40% keyword change".
**Solution:**
- Changed `overlap < 0.6` to `overlap <= 0.6` in the detection algorithm
- The spec says ">=40% keyword change" which means <=60% overlap (inclusive)
- Borderline test case: ['a','b','c','d','e'] → ['a','b','c','x','y'] = 60% overlap → switch
**Key Files/Paths:**
- examples/07-topic-switch/test.sh
- SKILL.md (Step 2: Topic Switch Detection)
**Related:**
**Keywords:** topic-switch, threshold, overlap, boundary, algorithm, inclusive

## init.sh creates blank indexes, not seeded ones
**Date:** 2026-02-19
**Skill:** mem-skill
**Source:** conversation
**Context:** Integration test 08-dynamic-category expected 5 starter categories after running init.sh, but init.sh creates empty `"categories": []`. The 5 starter categories in the repo's `knowledge-base/_index.json` are part of the packaged skill template, not generated by init.sh.
**Solution:**
- Fixed test to expect 0 categories after fresh init (not 5)
- Adjusted downstream count expectations (1 after adding, not 6)
- Architecture note: init.sh is for setting up a USER's workspace; the skill's own repo has pre-seeded indexes
**Key Files/Paths:**
- scripts/init.sh (creates empty categories/skills arrays)
- examples/08-dynamic-category/test.sh
- knowledge-base/_index.json (repo version has 5 starter categories)
**Related:**
**Keywords:** init, categories, blank, seeded, template, workspace

## Adding /mem-skill recordnow for manual recording
**Date:** 2026-02-23
**Skill:** mem-skill
**Source:** conversation
**Context:** In multi-task sessions, Step 5 (proactive recording) often doesn't trigger because the agent loses track of completion signals across many tasks. Users need a way to manually trigger recording after the fact.
**Solution:**
- Added `/mem-skill recordnow` command to SKILL.md as a new section before Core Loop
- Procedure: scan full conversation → summarize completed tasks → present numbered list → user picks which to save
- Uses the same write flow as Step 5 (including QMD post-write sync)
- Added cross-reference from Step 5: "user can run `/mem-skill recordnow` at any time"
- Updated README.md: Quick Start section + FAQ entry
**Key Files/Paths:**
- SKILL.md (Manual Recording Command section)
- README.md (Quick Start + FAQ)
**Related:**
**Keywords:** recordnow, manual, recording, multi-task, Step-5, trigger, command

## Implementing 7 LLM Wiki enhancements end-to-end
**Date:** 2026-04-08
**Skill:** mem-skill
**Source:** conversation
**Related:** [[workflow#External Best-Practice Review Pattern (Karpathy LLM Wiki)]], [[skill-mem-skill#Building the upgrade command with idempotent backfill]]
**Context:** After reviewing Karpathy's LLM Wiki, implemented all 7 identified enhancements in a single coordinated release: source provenance, cross-references, log.md, compounding updates, filing queries, lint command, and ingest command.
**Solution:**
- Added Source and Related fields to entry formats in SKILL.md with clear field-value docs
- Created log.md during init; appended log entries on read/write/lint/ingest/recordnow
- Modified Step 5 to search existing entries before writing (compounding updates instead of duplicates)
- Added `/mem-skill lint` with 6 health checks: duplicates, stale, contradictions, orphans, missing cross-refs, index consistency
- Added `/mem-skill ingest` for processing external files/URLs/directories into knowledge entries
- Updated init.sh to create log.md, updated integration test 01-init-default (file count 3→4)
- All changes coordinated: SKILL.md, README.md, init.sh, tests — bumped to v1.2.0
**Key Files/Paths:**
- SKILL.md (Entry Formats, Log Format, Core Loop steps, lint/ingest commands)
- README.md (What Gets Recorded, FAQ, Credits)
- scripts/init.sh (log.md creation)
- examples/01-init-default/test.sh (updated assertions)
**Keywords:** LLM-wiki, enhancement, source-provenance, cross-reference, log, compounding, lint, ingest

## Building the upgrade command with idempotent backfill
**Date:** 2026-04-08
**Skill:** mem-skill
**Source:** conversation
**Related:** [[workflow#Upgrade Path for Skill Versions]], [[workflow#sed-based Markdown Field Backfilling]], [[skill-mem-skill#Implementing 7 LLM Wiki enhancements end-to-end]]
**Context:** Existing v1.1.0 users needed to get v1.2.0 features (Source/Related fields, log.md) without re-initializing. Built `--upgrade` flag in init.sh with sed-based backfilling.
**Solution:**
- Added `--upgrade` flag that exits early before normal init flow (avoids accidental re-init)
- Reads current version and engine from `.mem-skill.config.json`
- Creates log.md if missing, backfills `**Source:** conversation` after `**Date:**` lines, backfills `**Related:**` before `**Keywords:**` lines
- Updates config version as last step so interrupted upgrades can be re-run
- Re-indexes QMD collections if engine is qmd
- Gotcha: first implementation accidentally added duplicate upgrade blocks (one at top, one at bottom of init.sh) — must keep only the early-exit version
- Tested end-to-end with /tmp fixture, verified idempotency (second run: 0 backfills)
**Key Files/Paths:**
- scripts/init.sh (--upgrade flag, early exit, sed backfill logic)
- SKILL.md (Upgrade Command section)
- README.md (Upgrading from v1.1.0 section)
**Keywords:** upgrade, backfill, idempotent, sed, migration, init, version-bump
